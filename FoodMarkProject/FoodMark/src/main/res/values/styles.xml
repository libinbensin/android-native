<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="spinner_menu_item" parent="android:Widget.TextView.SpinnerItem">
        <item name="android:dropDownSelector">@android:color/darker_gray</item>
    </style>

    <style name="dialog_light" parent="android:Theme.Holo.Light.Dialog">
        <item name="android:textColor">@color/Black</item>
        <item name="android:windowBackground">@color/WhiteGrey</item>
        <item name="android:gravity">center_horizontal</item>
    </style>

    <style name="AppButtonMapAction" parent="android:Widget.Holo.Light.Button.Small">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">38dp</item>
        <item name="android:background">@color/White</item>

    </style>

</resources>
